pipeline{
  agent any
   stages {
    stage('Checkout') {
      steps {
        // Clone code từ GitHub vào container
        checkout scm
      }
    }
  stages {
    stage('Build'){
      steps {
      echo 'Testing Pipeline .......'
          }
       }
    }
}
